apiVersion: v1
kind: ConfigMap
metadata:
  name: atonixcorp-config
data:
  DJANGO_ALLOWED_HOSTS: "api.atonixcorp.com"
  CORS_ALLOWED_ORIGINS: "https://app.atonixcorp.com"
  CSRF_TRUSTED_ORIGINS: "https://app.atonixcorp.com"
  DEPLOYMENT_ENVIRONMENT: "prod"
  APP_VERSION: "prod"