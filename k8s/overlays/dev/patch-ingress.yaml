apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: atonixcorp
spec:
  tls:
    - hosts:
        - app.dev.atonixcorp.com
      secretName: atonixcorp-dev-tls
  rules:
    - host: app.dev.atonixcorp.com
