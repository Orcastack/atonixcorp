apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base
  - namespace.yaml

patchesStrategicMerge:
  - patch-config.yaml
  - patch-ingress.yaml
  - patch-scale.yaml

images:
  - name: atonixcorp/backend
    newName: registry.example.com/atonixcorp/backend
    newTag: production
  - name: atonixcorp/app
    newName: registry.example.com/atonixcorp/app
    newTag: production
